name: Supabase wachhalten

on:
  schedule:
    - cron: '0 8 * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Datenbank anpingen
        run: curl -s "https://aivyaldejqdfbqsryjku.supabase.co/rest/v1/" > /dev/null
